---
title: Per-polygon Interaction
description: Using the Poly component for per-polygon click handlers, hover states, and CSS styling.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

Every polygon rendered by polycss is a real DOM element. You can attach standard event handlers, apply CSS classes, and inspect them in DevTools. The `Poly` component exposes this directly.

## The `Poly` component

`Poly` renders a single polygon as an `<img>` (UV-textured) or `<svg>` (flat color). It forwards all standard DOM props: `onClick`, `onMouseEnter`, `className`, `style`, `aria-*`, and more.

```tsx
import { PolyScene, Poly } from "@polycss/react";

const triangle: Vec3[] = [[0,0,0], [1,0,0], [0,1,0]];

<PolyScene>
  <Poly vertices={triangle} color="#ff0000" />
  <Poly vertices={triangle} color="#0000ff" texture="/wood.png" uvs={[...]} />
</PolyScene>
```

## Interactive per-polygon example

<Tabs syncKey="fw">
<TabItem label="React">
```tsx
import { useState } from "react";
import { PolyScene, PolyMesh, Poly } from "@polycss/react";
import type { Polygon } from "@polycss/react";

export function InteractiveMesh({ polygons }: { polygons: Polygon[] }) {
  const [hoveredId, setHoveredId] = useState<number | null>(null);

  return (
    <PolyScene rotX={65} rotY={45}>
      {polygons.map((p, i) => (
        <Poly
          key={i}
          {...p}
          onClick={() => alert(`clicked polygon ${i}`)}
          onMouseEnter={() => setHoveredId(i)}
          onMouseLeave={() => setHoveredId(null)}
          className={hoveredId === i ? "highlight" : ""}
          style={{ transition: "filter 0.2s" }}
        />
      ))}
    </PolyScene>
  );
}
```
```css
/* highlight.css */
.highlight { filter: brightness(1.5); }
```
</TabItem>
<TabItem label="Vue">
```vue
<template>
  <PolyScene :rot-x="65" :rot-y="45">
    <Poly
      v-for="(p, i) in polygons"
      :key="i"
      v-bind="p"
      @click="onClickPoly(i)"
      @mouseenter="hoveredId = i"
      @mouseleave="hoveredId = null"
      :class="{ highlight: hoveredId === i }"
    />
  </PolyScene>
</template>

<script setup lang="ts">
import { ref } from "vue";
import { PolyScene, Poly } from "@polycss/vue";
import type { Polygon } from "@polycss/vue";

defineProps<{ polygons: Polygon[] }>();
const hoveredId = ref<number | null>(null);
const onClickPoly = (i: number) => alert(`clicked polygon ${i}`);
</script>

<style>
.highlight { filter: brightness(1.5); }
</style>
```
</TabItem>
</Tabs>

## `PolyMesh` with render prop

Use a render prop (React) or scoped slot (Vue) to override individual polygons inside a loaded mesh:

<Tabs syncKey="fw">
<TabItem label="React">
```tsx
import { useState } from "react";
import { PolyScene, PolyMesh, Poly } from "@polycss/react";

export function SelectableMesh() {
  const [selected, setSelected] = useState<number | null>(null);

  return (
    <PolyScene rotX={65} rotY={45}>
      <PolyMesh src="/character.glb" position={[5, 0, 0]} scale={2}>
        {(polygon, index) => (
          <Poly
            {...polygon}
            onClick={() => setSelected(index)}
            className={selected === index ? "outlined" : ""}
          />
        )}
      </PolyMesh>
    </PolyScene>
  );
}
```
</TabItem>
<TabItem label="Vue">
```vue
<template>
  <PolyScene :rot-x="65" :rot-y="45">
    <PolyMesh src="/character.glb" :position="[5, 0, 0]" :scale="2"
      v-slot="{ polygon, index }">
      <Poly
        v-bind="polygon"
        @click="selected = index"
        :class="{ outlined: selected === index }"
      />
    </PolyMesh>
  </PolyScene>
</template>

<script setup lang="ts">
import { ref } from "vue";
import { PolyScene, PolyMesh, Poly } from "@polycss/vue";
const selected = ref<number | null>(null);
</script>
```
</TabItem>
</Tabs>

## `useMesh` hook — imperative loading

Load a mesh imperatively when you need finer control over loading state:

```tsx
import { PolyScene, Poly, useMesh } from "@polycss/react";

function Viewer() {
  const { polygons, loading, error, dispose } = useMesh("/cottage.glb");

  if (loading) return <Spinner />;
  if (error) return <div>Error: {error}</div>;

  return (
    <PolyScene>
      {polygons.map((p, i) => <Poly key={i} {...p} />)}
    </PolyScene>
  );
}
```

## Related

- [PolyScene](/components/poly-scene) — Scene props and polygon data reference.
- [Loading Meshes](/guides/textures) — OBJ, GLB, MTL loading and UV textures.
- [Performance](/guides/performance) — Merge modes and DOM tuning.
