---
title: Introduction
description: What glyphcss is and the problem it solves.
---

import GlyphDemo from "../../components/GlyphDemo.astro";

**glyphcss** is an ASCII polygon-mesh rendering engine for the DOM. It paints 3D meshes
(OBJ / glTF / GLB / VOX) into a character grid, then animates rotation via CSS
`steps(N)` keyframes. JavaScript runs once at scene init and again only when the user
drags or zooms — never per frame.

<GlyphDemo id="intro-demo" geometry="cuboctahedron" />

## The mental model

A glyphcss scene is **one DOM node per polygon** — every triangle is an absolutely
positioned `<div>` transformed with `matrix3d(...)`. The browser composites the result.

An **glyphcss** scene is **one `<pre>` per scene plus a sparse `<div>` hit layer**.
The `<pre>` contains N pre-baked rotation frames stacked vertically; CSS animates the
strip with `steps(N, end)` over a configurable duration. Hotspots (one per registered
3D anchor) get their own `@keyframes` that step through projected positions in sync
with the strip.

## Tradeoffs vs glyphcss

| | glyphcss | glyphcss |
|---|---|---|
| Cost dominator | polygon count | grid cell count |
| Per-polygon DOM events | ✓ (one node per polygon) | opt-in via `<GlyphHotspot>` |
| Texture / material support | full UVs, atlas, lighting | wireframe + Lambert ramp + voxel face |
| Per-frame JS work | none (CSS-driven) | none (CSS-driven) |
| Visual style | smooth-shaded | ASCII / glyph art |

glyphcss is the right tool when you want **iconic, terminal-aesthetic 3D** with
**handpicked interactivity** — not when you need photorealistic texture mapping.

## What's next

- [Quickstart](/quickstart) — npm-installable demo in 30 seconds.
- [Core Concepts](/core-concepts) — the strip flipbook, the hit layer, the camera contract.
- [Gallery](/gallery) — tune live, see the renderer at work.
