# Companion material file for coliseum.obj.
# Hand-authored — the original asset's mtllib (SM_Coliseum.mtl) doesn't ship,
# so every usemtl group fell back to a distinct default palette color (rainbow).
# Map all groups to one warm travertine stone so it reads as a single building.

newmtl lambert2SG
Kd 0.76 0.68 0.52

newmtl lambert3SG
Kd 0.76 0.68 0.52

newmtl lambert4SG
Kd 0.72 0.64 0.49

newmtl lambert5SG
Kd 0.80 0.72 0.56
