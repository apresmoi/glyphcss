{
  "name": "glyphcss-monorepo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "description": "Glyphcss ASCII polygon mesh rendering engine monorepo.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/apresmoi/glyphcss.git"
  },
  "bugs": {
    "url": "https://github.com/apresmoi/glyphcss/issues"
  },
  "homepage": "https://github.com/apresmoi/glyphcss#readme",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "build:packages": "pnpm --filter './packages/*' -r build",
    "test": "pnpm --filter './packages/*' -r --if-present test",
    "test:coverage": "pnpm --filter './packages/*' -r --if-present test:coverage",
    "publish:all": "pnpm --filter './packages/*' -r publish --access public",
    "dev:website": "pnpm --filter @glyphcss/website dev",
    "build:website": "pnpm --filter @glyphcss/website build"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "esbuild": "^0.28.0",
    "playwright": "^1.58.2",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "vue": "^3.5.30"
  }
}
