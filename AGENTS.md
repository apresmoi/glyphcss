# AGENTS

Minimal notes for Codex agents working in this repo.

- Run `npm run build` after making code changes.
- Run `npm run bench:test -- <vox-file>` for plane-shell diagnostics. Iterate based on results. Always aim for LOW DOM and HIGH FPS.
- Useful models: `examples/models/army.vox`, `examples/models/tank.vox`, `examples/models/bus.vox`.
